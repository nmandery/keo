dependencies {
    // additional dependencies
    compile "com.fasterxml.jackson.module:jackson-module-kotlin:$jackson_version"
    compile project(':keo-core')
}

sourceSets {
    main.kotlin.srcDirs += 'src/main/kotlin'
    test.kotlin.srcDirs += 'test/main/kotlin'
}
